# Use this file if running on heroku cloud
build:
  docker:
    web: Dockerfile
  config:
    CHROMEDRIVER_PATH: /app/.chromedriver/bin/chromedriver
    GOOGLE_CHROME_BIN: /app/.apt/usr/bin/google-chrome